export type Games = {
    id: string,
    name: string,
    description: string,
    rating: number,
    released: Date,
    price : number,
    background_image: string,
    tags: string,
    platforms: string[],
    developer: string,
    
}